language: groovy

dist: trusty

env:
  global:
    - GRADLE_OPTS=-Xmx1524m

jdk:
  - oraclejdk8
  - oraclejdk11
  - openjdk8
  - openjdk11
  - openjdk-ea  

script:
  - java -version
  - ./gradlew -version
  - ./gradlew --stop
  - ./gradlew --refresh-dependencies clean check test -Dscan --info
